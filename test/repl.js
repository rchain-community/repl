const assert = require('assert')
const REPL = require('../lib/REPL')

describe('eval', () => {
    it('should be able to execute rholang', () => {
      // FIXME
      assert.equal(true, true)
    })
})
